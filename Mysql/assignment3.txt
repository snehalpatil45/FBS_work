# Assignment 3

Q1.write an sql query to insert the above employee records into the employee table.
-> insert into employees
    -> (employee_id,first_name,last_name,job_id,salary,manager_id,department_id)
    -> values
    -> (100,'steven','king',1,24000.00,200,10),
    -> (101,'neena','kochhar',2,17000.00,200,10),
    -> (102,'lex','de haan',2,17000.00,200,10),
    -> (103,'alexander','hunold',3,9000.00,103,60),
    -> (104,'bruce','ernst',3,6000.00,103,60),
    -> (105,'david','austin',3,4800.00,103,60),
    -> (106,'valli','pataballa',3,4800.00,103,60),
    -> (107,'diana','lorentz',3,4200.00,103,60),
    -> (108,'nancy','greenberg',4,12000.00,114,80),
    -> (109,'daniel','faviet',4,9000.00,145,80);

Q2.write a single sql insert statement to insert atleast 3 department records into the departments table.
-> mysql> insert into departments
    -> (department_id,department_name,manager_id,location_id)
    -> values
    -> (40,'foods',202,1500),
    -> (50,'electrical',204,1900),
    -> (90,'management',205,1600);

Q3.write an sql query to insert one new job record into the jobs table ensuring duplicate job_id is not allowed.
-> insert into jobs
    -> values
    -> (5,'devloper',34000,45000);
    ->alter table jobs
    -> add unique(job_id);

Q4. write an sql query to increment the salary of each employee by 10%.
->  update employee
    -> set salary = salary * 1.10;

Q5. write an sql query to set email = not available for employees whose department_id is 80 and commision_pct is less than 0.20.
-> mysql> update employees
    -> set email = 'not available'
    -> where department_id = 80
    -> and
    -> commission_pct < 0.20;

Q6. write an sql query to set email = 'not available' for employees who belong to the purchasing department.
-> mysql> update employees
    -> set email = 'not available'
    -> where department_id = (select department_id from departments
    -> where department_name = 'purchasing');

Q7. write an sql query to update email = 'not available' and commission_pct = 0.10 for all employees.
-> mysql> update employees
    -> set email = 'not available',
    -> commission_pct = 0.10 ;

Q8. write an sql query to increase salary by 2000 for employees working in department_id = 60.
-> 
mysql> update employees
    -> set salary = salary + 2000
    -> where department_id = 60;

Q9. write an sql query to delete employees whose salary is less than 5000.
-> mysql> delete from employees
    -> where salary < 5000;

Q10. write an sql query to insert one new employee record with your own suitable details(use a new employee_id)
-> mysql> insert into employees
    -> (employee_id , first_name,last_name,job_id,salary,manager_id,department_id)
    -> values
    -> (110,'alex','smith',3,5500,103,60);

Q11. write an sql query to change manager_id to 300 for all employees in department_id = 80.
-> mysql> update employees
    -> set manager_id = 300
    -> where department_id = 80;

Q12.write an sql query to delete the department record where department_id = 20 and observe the error if employees exist in that department.
-> mysql> delete from departments
    -> where department_id = 20;