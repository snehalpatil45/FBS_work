# Assignment 4

Q1. write an sql query to display employee first name and last name with sutaible column aliases.
-> mysql> select first_name as 'First Name',last_name as 'Last Name' from employees;

Q2. write an sql query to display all diffrent department ids available in the employees table.
-> mysql> select distinct department_id from employees;

Q3. write an sql query to display all employee details sorted by first_name in descending order.
-> mysql> select * from employees
    -> order by first_name desc;

Q4. write an sql query to display first_name , last_name,salary and pf for all employees(pf is calculated as 15% of salary).
-> mysql> select first_name,last_name,salary,(salary * 0.15) as pf from employees;

Q5. write an sql query to display employee_id ,employee_name and salary sorted in increasing order of salary.
-> mysql> select employee_id,first_name,last_name,salary from employees
    -> order by salary asc;

Q6. write a sql query to display the total salary payable to all employee.
-> mysql> select sum(salary) as 'total salary payable' from employees;

Q7. write a sql query to display highest salary and lowest salary from employee table.
-> mysql> select max(salary) ,min(salary) from employees;

Q8. write a sql query to display the average salary and total number of employees working in the company.
-> mysql> select avg(salary),count(employee_id) from employees;

Q9. write a sql query to display the total number of employees working in the company.
-> mysql> select count(employee_id) from employees;

Q10. write a sql query to display no.of diffrent job roles available in the employee table .
-> mysql> select count(distinct job_id) from employees;

Q11. write a sql query to display first 10 employee records from the employee table.
-> mysql> select * from employees limit 10;

Q12. write a sql query to display employee name and salary for employees whose salary is less than 10000 and greater than 15000.
-> mysql> select first_name,last_name,salary from employees
    -> where salary between 10000 and 15000;

Q13. write a sql query to display employee names and department_id  for employees working in department 30 and  100 .
sort the result by department_id
-> mysql> select first_name,last_name,department_id
    -> from employees
    -> where department_id in (30 , 100)
    -> order by department_id ;

Q14. write a sql query to display  employee names and salary for employees working in department 30 and 100
whose salary is outside the range 10000 to 15000
-> mysql> select first_name,last_name,salary from employees
    -> where department_id in (30 , 100)
    -> and (salary < 10000 or salary > 15000);

Q15. write a sql query to display employee names and hire date for employees who were hired in the year 1987.
-> mysql> select first_name,last_name,hire_date from employees
    -> where year(hire_date) = 1987;

Q16. write a sql query to display first name of employees whose first name contains both letters b and c.
-> mysql> select first_name from employees
    -> where first_name like '%b%' and first_name like '%c%';

Q17. write an sql query to display last_name,job role and salary for employees who work as a programmer or shipping clerk and whose salary is not 
equal to 4500,10000 or 15000.
-> mysql> select last_name,job_id,salary from employees
    -> where job_id in ('programmer','shipping_clerk')
    -> and salary not in(4500,10000,15000); 

Q18.write an sql query to diplay last name of employees whose last name contain exactly 6 characters.
-> mysql> select last_name from employees
    -> where last_name like '______';

Q19.write an sql query to display last name of employees having 'e' as 3rd character in last name.
-> mysql> select last_name from employees
    -> where last_name like '__e%';

Q20. write an sql query to display all diffrent job roles/designation available in the employees table.
-> mysql> select distinct job_id from employees;

Q21. write an sql query to display employee details for employees who's last name is blake,scott,king or ford.
-> mysql> select * from employees
    -> where last_name in ('blake','scott','king','ford');