MySQL Assignment-2

Q1.create the member,books,and issue without constraints.
-> mysql> create table Member(
    -> member_id int,
    -> member_name varchar(30),
    -> member_address varchar(50),
    -> acc_open_date date,
    -> membership_type varchar(20),
    -> fees_paid decimal(10,2),
    -> max_books_allowed int,
    -> penalty_amount decimal(10,2));
Query OK, 0 rows affected (3.438 sec)

mysql> create table books(
    -> book_no int,
    -> book_name varchar(30),
    -> author_name varchar(30),
    -> cost decimal(10,2),
    -> category varchar(15));
Query OK, 0 rows affected (0.192 sec)

mysql> create table issue(
    -> lib_issue_id int,
    -> book_no int,
    -> member_id int,
    -> issue_date date,
    -> return_date date);
Query OK, 0 rows affected (0.197 sec)

Q2.Display the stucture of all three tables using desc.
-> mysql> desc member;
+-------------------+---------------+------+-----+---------+-------+
| Field             | Type          | Null | Key | Default | Extra |
+-------------------+---------------+------+-----+---------+-------+
| member_id         | int           | YES  |     | NULL    |       |
| member_name       | varchar(30)   | YES  |     | NULL    |       |
| member_address    | varchar(50)   | YES  |     | NULL    |       |
| acc_open_date     | date          | YES  |     | NULL    |       |
| membership_type   | varchar(20)   | YES  |     | NULL    |       |
| fees_paid         | decimal(10,2) | YES  |     | NULL    |       |
| max_books_allowed | int           | YES  |     | NULL    |       |
| penalty_amount    | decimal(10,2) | YES  |     | NULL    |       |
+-------------------+---------------+------+-----+---------+-------+
8 rows in set (0.795 sec)

mysql> desc books;
+-------------+---------------+------+-----+---------+-------+
| Field       | Type          | Null | Key | Default | Extra |
+-------------+---------------+------+-----+---------+-------+
| book_no     | int           | YES  |     | NULL    |       |
| book_name   | varchar(30)   | YES  |     | NULL    |       |
| author_name | varchar(30)   | YES  |     | NULL    |       |
| cost        | decimal(10,2) | YES  |     | NULL    |       |
| category    | varchar(15)   | YES  |     | NULL    |       |
+-------------+---------------+------+-----+---------+-------+
5 rows in set (0.039 sec)

mysql> desc issue;
+--------------+------+------+-----+---------+-------+
| Field        | Type | Null | Key | Default | Extra |
+--------------+------+------+-----+---------+-------+
| lib_issue_id | int  | YES  |     | NULL    |       |
| book_no      | int  | YES  |     | NULL    |       |
| member_id    | int  | YES  |     | NULL    |       |
| issue_date   | date | YES  |     | NULL    |       |
| return_date  | date | YES  |     | NULL    |       |
+--------------+------+------+-----+---------+-------+
5 rows in set (0.034 sec)

Q3.drop the member table.
-> mysql> drop table member;
Query OK, 0 rows affected (0.230 sec)

Q4.create member table again with constraints.
-> mysql> create table member(
    -> member_id int primary key,
    -> member_name varchar(50),
    -> address varchar(100),
    -> phone_no varchar(15),
    -> membership_type varchar(10),
    -> join_date date,
    -> check(membership_type in ('lifetime','annual','half')));

Q5.alter member table to increase member name size from 30 to 40.
-> mysql> alter table member
        -> modify column member_name varchar(40);

Q6.add a column reference (varchar(30)) to issue table.
-> mysql> alter table issue
       -> add reference varchar(30);

Q7.drop the column reference from issue table.
-> mysql> alter table issue
    -> drop column reference;

Q8.rename the table issue to lib_issue.
-> mysql> rename table issue to lib_issue;

Q9.insert the following records into member table.
-> mysql> insert into member values
    -> (1,'richa sharma','pune','10-12-05','lifetime',25000,5,50),
    -> (2,'garima sen','pune','12-3-26','annual',1000,3,null);

Q10.insert atleast 5 records.
-> mysql> insert into member values
        -> (3,'rohit sharma','mumbai','4-5-05','lifetime',5000,2,null),
        -> (4,'mohini jadhav','nashik','12-6-26','annual',15000,6,100),
        -> (5,'snehal patil','dhule','10-8-26','half yearly',56000,67,null),
        -> (6,'ashwini jadhav','satara','3-8-25','quarterly',15985,678,1000),
        -> (7,'vijay sharma','sangli','10-1-07','lifetime',23900,57,5090);

Q11.Try to decrease Member_Name size from 40 to 20.
If it fails, mention the reason.
-> mysql> alter table member
    -> modify member_name varchar(20);

Q12.add check constraint
max_book_allowed must be less than 100.
-> mysql> alter table member
    -> modify column max_books_allowed int check (max_books_allowed < 100);

Q13. create backup table named memberbackup using member table.
-> mysql> create table memberbackup as
    -> select * from member;

Q14. add named constraints 
max_books allowed < 100
penalty_amount <= 1000
-> mysql> alter table member
    -> modify column penalty_amount decimal(10,2) check (penalty_amount <= 1000);

Q15.drop the book table.
-> mysql> drop table books;

Q16.create book table again with constraints.
-> mysql> create table book(
    -> book_no int primary key,
    -> book_name varchar(30) not null,
    -> cost decimal(10,2),
    -> category varchar(15),
    -> check( category in ('fiction','database','rdbms','others','science')));

Q17.insert the following data into book table.
-> mysql> insert into book values
    -> (101,'let us c','denis ritchie',450,'system'),
    -> (102,'oracle complete ref','loni',550,'database'),
    -> (103,'mastering sql','loni',250,'database'),
    -> (104,'pl sql ref','scott umran',750,'database'),
    -> (105,'national geographic','adis scott',1000,'science');

Q18.insert atleast 5 records in book.
-> mysql>(106,'operating system','tanenbaum',650,'system'),
    -> (107,'c programming','dennis ritchie',500,'system'),
    -> (108,'sql fundamentals','john watson',600,'rdbms'),
    -> (109,'science world','james clark',700,'science'),
    -> (110,'story book','mark lee',300,'fiction');

Q19.view all data from member and books.
->mysql> table member;
mysql> table book;
Q20.drop lib_issue table.
-> mysql> drop table lib_issue;

Q21. create issue table with constraints.
->create table issue(
    -> lib_issue_id int primary key,
    -> book_no int,
    -> member_id int,
    -> issue_date date,
    -> foreign key(book_no)
    -> references book(book_no),
    -> foreign key(member_id)
    -> references member(member_id));

Q22.insert records into issue table.
-> mysql> insert into issue values
    -> ( 7001,101,1,'2024-12-10'),
    -> ( 7002,102,2,'2024-12-25'),
    -> ( 7003,103,3,'2024-1-15'),
    -> ( 7004,104,4,'2025-2-4'),
    -> ( 7005,101,3,'2025-3-15'),
    -> ( 7006,101,3,'2025-4-18');

Q23.Insert Issue with Member_id not existing
-> mysql>insert into issue values (7007,101,10,'2025-05-01');

Q24.Delete Member_id = 1
-> mysql>delete from member
where member_id = 1;

Q25.Update Return_Date
-> mysql>update issue
set return_date = issue_date + interval 15 day
where lib_issue_Id in (7004,7005);

Q26.Delete Issue records
-> mysql>delete from issue
where member_id = 1
and issue_Date < '2006-12-10';

Q27.Delete books where Category is not Database or RDBMS
-> mysql>delete from book
where category not in ('database','rdbms');

Q28.Drop all three tables
-> mysql>drop table issue;
drop table member;
drop table book;
